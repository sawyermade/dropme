PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# Leave empty to serve from the domain root. Set to a path prefix (no trailing
# slash) when hosting under an existing site, e.g. BASE_PATH=/dropme for
# https://example.com/dropme
BASE_PATH=
